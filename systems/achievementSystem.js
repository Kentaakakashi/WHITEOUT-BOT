const logger = require("../utils/logger");

async function initializeAchievementSystem(client) {
    logger.info("Achievement system initialized.");
}

module.exports = {
    initializeAchievementSystem
};
